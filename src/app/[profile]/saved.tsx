import ProfileBase from "../../components/ProfileBase";

export default function ProfileSaved() {
  return (
    <ProfileBase>
      <div className="grid grid-cols-3 gap-1">
        {/* Add saved posts content here */}
        <p>Saved posts will be displayed here</p>
      </div>
    </ProfileBase>
  );
}
